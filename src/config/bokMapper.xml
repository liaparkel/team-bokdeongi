<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bokMapper">
	<select id="getSprtPerson" resultType="com.bok.model.SprtPersonVO">
	SELECT sprt_title AS title, sprt_link AS link, sprt_summary AS summary, sprt_start AS "start", sprt_end AS "end" FROM sprt_person WHERE sprt_num = #{fkSprtNum} AND sprt_end >= TRUNC(SYSDATE)
	</select>

</mapper>
